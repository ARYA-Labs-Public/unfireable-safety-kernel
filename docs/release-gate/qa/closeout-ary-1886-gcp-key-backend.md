# Closeout Rollup — ARY-1886 Step-14R GCP key backend (slice)

- **Wave**: `ary1886-gcp-key-backend`
- **Originating issue**: ARY-1886 (Phase-4 self-hosted deployment / Step-14R key management)
- **Repo**: `ARYA-Labs-PBC/unfireable-safety-kernel`, branch `seth/ary-1886-gcp-key-backend`
- **Release-gating**: this is a slice; ARY-1886 stays **In Progress** (does NOT close Done).
- **Closeout verdict**: PASS (slice scope) — ship the slice; remaining ACs tracked.

## What this slice delivers
`KERNEL_KEY_BACKEND` pluggable signing-key abstraction for the Rust Safety Kernel:
- `env` backend (default) — **blocked in prod** (fail-closed runtime guard).
- `gcp` backend — GCP Secret Manager fetch via metadata-server ADC; **live-verified** end-to-end on GCE.
- `safety-kernel-keygen` — Ed25519 first-boot seed generator (operator/Terraform provisioning; least-privilege read-only kernel).
- `aws`/`azure`/`pkcs11`/`tpm` — fail-closed with a tracking pointer (ARY-2518 / 2519 / 2520 / 2521).

## Delegated skills (every one ran + PASS)
| Skill | Session | Verdict | Evidence |
|-------|---------|---------|----------|
| `/test` | `ary1886-test-0004e89-gcpkey` | PASS | build + full suite + Rule-8 reject fixtures + Rule-9 live GCP byte-equal + pubkey-fp + vacuity probe (wrong seed FAILS). No blocking findings. |
| `/purple-team` | `ary1886-pt-gcpkeybackend` | PASS | 6 threats assessed; 0 unmitigated high/critical. PT-1 (medium) + PT-3 (info) **remediated in-wave**; PT-2 (low) deferred to rotation work. |
| `/user-acceptance` | inline | PARTIAL (slice) | per-AC matrix below; honest deviations flagged. |
| `/closeout` | `ary1886-closeout-gcpkey` | PASS | this report. |

## Purple-team finding disposition
- **PT-1 (Medium)** `QORCH_ENV` not trimmed → `" prod"` bypasses the prod-guard + 4 fail-closed gates. **FIXED**: `env_v.trim().to_ascii_lowercase()` in `settings.rs` + new whitespace-prod adversarial fixture in `key_backend_prod_guard.rs` (verified rejecting).
- **PT-3 (Info)** no reqwest timeout → hung endpoint stalls boot. **FIXED**: 10s (Secret Manager) / 5s (metadata) timeouts.
- **PT-2 (Low)** `KERNEL_KEY_GCP_SECRET_VERSION` defaults to `latest`, no pin. **Deferred** — version-pinning + dual-sign rotation + `KERNEL_KEY_REVOKED` are explicitly ARY-1886 Step-14R rotation/revocation scope, documented in `key-management.md`.

## Per-AC verdict (ARY-1886, slice-relevant only)
| AC | Verdict | Note |
|----|---------|------|
| AC7(R) — all 5 key backends | **PARTIAL** | `env` + `gcp` done + live-verified; `aws`/`azure`/`pkcs11`/`tpm` fail-closed + tracked (ARY-2518–2521). Slice, by design. |
| Step-14R env-backend-blocked-in-prod | **PARTIAL→PASS(runtime)** | Runtime guard done + tested (incl. whitespace). The AC's *additional* compile-time feature-gate is **deferred** (noted in `key-management.md`); runtime is the load-bearing control. |
| AC6(R) — Ed25519 generated by `key_init.rs` on first boot | **DEVIATION (documented)** | Implemented as `safety-kernel-keygen` operator tool + read-only kernel, NOT kernel-auto-generate-on-first-boot. Rationale: auto-gen-on-first-boot needs the kernel SA to hold secret-**write** IAM (or write plaintext to disk), violating least privilege. The split (operator/Terraform writes, kernel reads) is the stronger security posture. Flagged for Seth's acceptance rather than silently claimed. |
| AC1(R)/AC9(R)/AC15(R) (image/client-pin/latency) | **N/A this slice** | Untouched by the key-backend change. |

## Why ARY-1886 stays In Progress
The slice is one column of Step-14R (GCP) + the prod guard + keygen. The remaining backends (4 issues), rotation/revocation, the compile-time guard, and the Step-13R packaging deliverables (systemd unit, helm chart, threat-model.md, per-target deploy docs) are not in this slice. Do not transition ARY-1886 to Done.

## Trailers
```
Adversarial-Suite: ary1886-test-0004e89-gcpkey PASS
Purple-Team: ary1886-pt-gcpkeybackend PASS
Closeout: ary1886-closeout-gcpkey PASS
```
